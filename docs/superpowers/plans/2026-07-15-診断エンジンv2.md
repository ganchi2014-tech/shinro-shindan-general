# 診断エンジンv2（フェーズ2）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 学部単位データ（`work/data_v2.json`）から、地域ハードフィルタ＋RIASEC適性＋挑戦/実力/安全3段で学部をランキングする純粋モジュール `work/engine_v2.js` を、テスト付きで実装する。

**Architecture:** 依存パッケージなしのNode純粋関数モジュール。公開API `diagnose()` の内部を「①ハードフィルタ→②ゾーン分類→③適合スコア→④整形」の独立小関数に分割し各々を単体テスト。偏差値はゾーン分類だけに使い、適合スコア(fit)とは分離する。v1（`進路診断_一般版.html`/`work/app.html`）と `data_v2.json` 本体は無変更（engineは読み取り専用）。

**Tech Stack:** Node.js（既存 `work/` と同じ、依存なし）。テストは素のassert（`node test_engine_v2.js`）。確認用に単一HTMLハーネス。

**Spec:** `docs/superpowers/specs/2026-07-15-診断エンジンv2-design.md`

**環境注意:**
- 起点は Git Bash で `cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版/work"`
- OneDrive配下のため書き込み後は必要に応じ `ls`/`node` で実在確認
- コミットは親リポ（`進路診断_一般版`）で行う。`site/` は除外済み

---

### Task 1: 定義データ（RIASEC質問票 と interest×RIASEC 重み表）

**Files:**
- Create: `work/riasec_def.json`
- Create: `work/interest_riasec_map.json`

- [ ] **Step 1: riasec_def.json を作成**

設問の並び順は「R,R,I,I,A,A,S,S,E,E,C,C」の正準順（エンジンはこの順に依存する）。

```json
{
  "version": "v2-2026-07",
  "scale": { "max": 3, "labels": ["当てはまらない", "あまり", "やや", "とても当てはまる"] },
  "items": [
    { "id": "R1", "type": "R", "text": "機械や道具を分解・修理したり、組み立てたりするのが好きだ" },
    { "id": "R2", "type": "R", "text": "体を動かす作業や、屋外・現場での活動にやりがいを感じる" },
    { "id": "I1", "type": "I", "text": "物事の仕組みや「なぜそうなるか」を突き止めるのが好きだ" },
    { "id": "I2", "type": "I", "text": "データや実験で仮説を確かめる作業が苦にならない" },
    { "id": "A1", "type": "A", "text": "絵・音楽・文章・映像など、何かを表現するのが好きだ" },
    { "id": "A2", "type": "A", "text": "決まったやり方より、自由な発想で作る方が向いている" },
    { "id": "S1", "type": "S", "text": "人に教えたり、相談に乗って助けたりするのが好きだ" },
    { "id": "S2", "type": "S", "text": "誰かの役に立てたと感じられる仕事にやりがいを感じる" },
    { "id": "E1", "type": "E", "text": "人をまとめたり、企画を提案してリードするのが好きだ" },
    { "id": "E2", "type": "E", "text": "目標を決めて、競争しながら成果を出すのにワクワクする" },
    { "id": "C1", "type": "C", "text": "計画を立て、正確・丁寧に物事を進めるのが得意だ" },
    { "id": "C2", "type": "C", "text": "数字やルールに沿って、こつこつ整理・管理する作業が好きだ" }
  ]
}
```

- [ ] **Step 2: interest_riasec_map.json を作成**

```json
{
  "version": "v2-2026-07",
  "note": "interest_tag(tags_def.json)ごとのRIASEC重み。複数タグ持ちの学部は平均。unclassifiedは全0。",
  "map": {
    "bungaku":   { "R": 0.0, "I": 0.6, "A": 0.7, "S": 0.3, "E": 0.1, "C": 0.1 },
    "gaikokugo": { "R": 0.0, "I": 0.3, "A": 0.5, "S": 0.6, "E": 0.5, "C": 0.2 },
    "hokei":     { "R": 0.0, "I": 0.4, "A": 0.1, "S": 0.5, "E": 0.7, "C": 0.6 },
    "keizai":    { "R": 0.0, "I": 0.4, "A": 0.1, "S": 0.3, "E": 0.8, "C": 0.6 },
    "shakai":    { "R": 0.0, "I": 0.5, "A": 0.5, "S": 0.7, "E": 0.4, "C": 0.2 },
    "kyoiku":    { "R": 0.1, "I": 0.3, "A": 0.4, "S": 0.9, "E": 0.3, "C": 0.3 },
    "riko":      { "R": 0.7, "I": 0.8, "A": 0.1, "S": 0.1, "E": 0.2, "C": 0.4 },
    "joho":      { "R": 0.4, "I": 0.8, "A": 0.2, "S": 0.1, "E": 0.3, "C": 0.6 },
    "kenchiku":  { "R": 0.6, "I": 0.5, "A": 0.7, "S": 0.2, "E": 0.3, "C": 0.3 },
    "nogaku":    { "R": 0.7, "I": 0.7, "A": 0.1, "S": 0.3, "E": 0.2, "C": 0.4 },
    "iryo":      { "R": 0.4, "I": 0.7, "A": 0.1, "S": 0.7, "E": 0.2, "C": 0.5 },
    "eiyo":      { "R": 0.4, "I": 0.4, "A": 0.3, "S": 0.6, "E": 0.2, "C": 0.6 },
    "sports":    { "R": 0.8, "I": 0.3, "A": 0.2, "S": 0.6, "E": 0.5, "C": 0.2 },
    "geijutsu":  { "R": 0.3, "I": 0.1, "A": 0.9, "S": 0.3, "E": 0.2, "C": 0.1 },
    "unclassified": { "R": 0.0, "I": 0.0, "A": 0.0, "S": 0.0, "E": 0.0, "C": 0.0 }
  }
}
```

- [ ] **Step 3: JSON妥当性と網羅を確認**

Run:
```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版/work"
node -e "
const r = require('./riasec_def.json'); const m = require('./interest_riasec_map.json'); const t = require('./tags_def.json');
console.log('items:', r.items.length);
const order = r.items.map(x => x.type).join('');
console.log('order:', order, order === 'RRIIAASSEECC' ? 'OK' : 'NG');
const tagIds = t.interest.map(x => x.id);
const missing = tagIds.filter(id => !m.map[id]);
console.log('mapに無いタグ:', missing.length ? missing : 'なし', '/ unclassified有:', !!m.map.unclassified);
"
```
Expected: `items: 12` / `order: RRIIAASSEECC OK` / `mapに無いタグ: なし` / `unclassified有: true`

- [ ] **Step 4: コミット**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git add work/riasec_def.json work/interest_riasec_map.json
git commit -m "feat(engine): RIASEC質問票12問とinterest×RIASEC重み表"
```

---

### Task 2: engine_v2.js スキャフォールド と computeRiasecProfile

**Files:**
- Create: `work/engine_v2.js`
- Create: `work/test_engine_v2.js`

`computeRiasecProfile(answers)` は正準順（Task 1）に依存し、riasec_def.json を引数に取らない（indexでtype決定）。

- [ ] **Step 1: 失敗するテストを書く（test_engine_v2.js を新規作成）**

```js
// work/test_engine_v2.js — node test_engine_v2.js で実行
const assert = require('assert');
const E = require('./engine_v2.js');

// ---- computeRiasecProfile ----
{
  // R2問=3,3 / I2問=3,0 / 他0 → R=1.0, I=0.5, 他0。hollandCode先頭はR
  const ans = [3, 3, 3, 0, 0, 0, 0, 0, 0, 0, 0, 0];
  const { profile, hollandCode, answered } = E.computeRiasecProfile(ans);
  assert.strictEqual(answered, true);
  assert.strictEqual(profile.R, 1.0);
  assert.ok(Math.abs(profile.I - 0.5) < 1e-9, 'I=0.5 expected: ' + profile.I);
  assert.strictEqual(profile.A, 0);
  assert.strictEqual(hollandCode[0], 'R');
  assert.strictEqual(hollandCode.length, 3);
}
{
  // 未回答 → 全0・answered=false
  const { profile, hollandCode, answered } = E.computeRiasecProfile([]);
  assert.strictEqual(answered, false);
  assert.strictEqual(hollandCode, '');
  assert.strictEqual(profile.S, 0);
}

console.log('ALL OK');
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `node test_engine_v2.js`
Expected: FAIL（`Cannot find module './engine_v2.js'`）

- [ ] **Step 3: engine_v2.js を作成（定数＋computeRiasecProfile）**

```js
#!/usr/bin/env node
// 進路診断v2 診断エンジン（純粋モジュール、依存なし）
// spec: docs/superpowers/specs/2026-07-15-診断エンジンv2-design.md

const RIASEC_TYPES = ['R', 'I', 'A', 'S', 'E', 'C'];
// riasec_def.json の items 正準順に対応（R,R,I,I,A,A,S,S,E,E,C,C）
const RIASEC_ORDER = ['R', 'R', 'I', 'I', 'A', 'A', 'S', 'S', 'E', 'E', 'C', 'C'];

// 模試種別→河合換算オフセット（v1 EXAM_SOURCE_OFFSET準拠。Task3で突き合わせ）
const EXAM_SOURCE_OFFSET = { kawai: 0, zento: 0, shinken: -5, sundai: 2, toshin: -3, unknown: 0 };

// ゾーン閾値（gap = 学部偏差値mid − 換算偏差値）
const ZONE = { CHALLENGE_MAX: 10, CHALLENGE_MIN: 2.5, MATCH_MIN: -2.5, SAFE_MIN: -10 };
const DEFAULT_TOP_N = 12;

const round1 = (x) => Math.round(x * 10) / 10;
const round2 = (x) => Math.round(x * 100) / 100;

// answers: 12要素(0-3)、正準順。空/undefinedなら未回答
function computeRiasecProfile(answers) {
  const profile = { R: 0, I: 0, A: 0, S: 0, E: 0, C: 0 };
  if (!answers || answers.length === 0) return { profile, hollandCode: '', answered: false };
  const sums = { R: 0, I: 0, A: 0, S: 0, E: 0, C: 0 };
  RIASEC_ORDER.forEach((t, i) => { sums[t] += Number(answers[i]) || 0; });
  for (const t of RIASEC_TYPES) profile[t] = sums[t] / 6; // 2問×最大3=6
  const hollandCode = [...RIASEC_TYPES].sort((a, b) => profile[b] - profile[a]).slice(0, 3).join('');
  return { profile, hollandCode, answered: true };
}

module.exports = {
  RIASEC_TYPES, RIASEC_ORDER, EXAM_SOURCE_OFFSET, ZONE, DEFAULT_TOP_N,
  computeRiasecProfile,
};
```

- [ ] **Step 4: テストが通ることを確認**

Run: `node test_engine_v2.js`
Expected: `ALL OK`

- [ ] **Step 5: コミット**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git add work/engine_v2.js work/test_engine_v2.js
git commit -m "feat(engine): computeRiasecProfile（12回答→6型プロフィール）"
```

---

### Task 3: adjustedHensachi と classifyZone

**Files:**
- Modify: `work/engine_v2.js`
- Modify: `work/test_engine_v2.js`

- [ ] **Step 1: v1のEXAM_SOURCE_OFFSETを突き合わせる**

Run:
```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
grep -oE "EXAM_SOURCE_OFFSET\s*=\s*\{[^}]*\}" work/app.html | head -1
```
Expected: v1の定義が表示される。**engine_v2.js の EXAM_SOURCE_OFFSET を v1 の値・キー名に合わせて修正する**（v1に無いキーは現状値を残す）。表示が空なら現状値のまま進む。

- [ ] **Step 2: 失敗するテストを追記（test_engine_v2.js の `console.log('ALL OK');` の直前に挿入）**

```js
// ---- adjustedHensachi ----
{
  assert.strictEqual(E.adjustedHensachi(55, 'kawai'), 55);
  assert.strictEqual(E.adjustedHensachi(55, 'shinken'), 50); // 進研は-5換算（値はv1準拠に合わせる）
  assert.strictEqual(E.adjustedHensachi(null, 'kawai'), null);
  assert.strictEqual(E.adjustedHensachi(55, 'unknownsrc'), 55); // 未知キーは0オフセット
}
// ---- classifyZone ----
{
  const adj = 50;
  assert.strictEqual(E.classifyZone(52.5, adj), 'challenge'); // gap+2.5
  assert.strictEqual(E.classifyZone(60, adj), 'challenge');   // gap+10
  assert.strictEqual(E.classifyZone(52.4, adj), 'match');     // gap+2.4
  assert.strictEqual(E.classifyZone(47.5, adj), 'match');     // gap-2.5
  assert.strictEqual(E.classifyZone(47.4, adj), 'safe');      // gap-2.6
  assert.strictEqual(E.classifyZone(40, adj), 'safe');        // gap-10
  assert.strictEqual(E.classifyZone(60.1, adj), 'out');       // gap>+10
  assert.strictEqual(E.classifyZone(39.9, adj), 'out');       // gap<-10
  assert.strictEqual(E.classifyZone(55, null), 'ungrouped');  // 偏差値未入力
}
```

Note: shinken の期待値(50)は Step 1 で確定した v1 オフセットに合わせて書き換えること（v1が-5でなければその値に）。

- [ ] **Step 3: テストが失敗することを確認**

Run: `node test_engine_v2.js`
Expected: FAIL（`E.adjustedHensachi is not a function`）

- [ ] **Step 4: engine_v2.js に2関数を追加（module.exports の前に挿入し、exportにも追加）**

```js
function adjustedHensachi(hensachi, examSource) {
  if (hensachi == null) return null;
  const off = EXAM_SOURCE_OFFSET[examSource];
  return hensachi + (off == null ? 0 : off);
}

// mid: 学部偏差値の中央値, adjHen: 換算偏差値(null可)
function classifyZone(mid, adjHen) {
  if (adjHen == null || mid == null) return adjHen == null ? 'ungrouped' : 'out';
  const gap = mid - adjHen;
  if (gap > ZONE.CHALLENGE_MAX || gap < ZONE.SAFE_MIN) return 'out';
  if (gap >= ZONE.CHALLENGE_MIN) return 'challenge';
  if (gap >= ZONE.MATCH_MIN) return 'match';
  return 'safe';
}
```

exportに `adjustedHensachi, classifyZone` を追加：

```js
module.exports = {
  RIASEC_TYPES, RIASEC_ORDER, EXAM_SOURCE_OFFSET, ZONE, DEFAULT_TOP_N,
  computeRiasecProfile, adjustedHensachi, classifyZone,
};
```

- [ ] **Step 5: テストが通ることを確認**

Run: `node test_engine_v2.js`
Expected: `ALL OK`

- [ ] **Step 6: コミット**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git add work/engine_v2.js work/test_engine_v2.js
git commit -m "feat(engine): adjustedHensachi と classifyZone（挑戦/実力/安全）"
```

---

### Task 4: hardFilter

**Files:**
- Modify: `work/engine_v2.js`
- Modify: `work/test_engine_v2.js`

- [ ] **Step 1: 失敗するテストを追記（`console.log('ALL OK');` の直前）**

```js
// ---- hardFilter ----
{
  const universities = [
    { id: 'ushiga', name: '滋賀大', region: 'shiga', commute_possible: true },
    { id: 'ukanto', name: '関東大', region: 'kanto', commute_possible: false },
  ];
  const departments = [
    { dept_id: 'ushiga-a', uni_id: 'ushiga', data_status: 'verified' },
    { dept_id: 'ushiga-b', uni_id: 'ushiga', data_status: 'unconfirmed' },
    { dept_id: 'ukanto-a', uni_id: 'ukanto', data_status: 'verified' },
  ];
  // 滋賀のみ選択 → 関東除外・unconfirmed除外 → 通過1件
  const r1 = E.hardFilter(departments, universities, { regions: ['shiga'] });
  assert.strictEqual(r1.passed.length, 1);
  assert.strictEqual(r1.passed[0].dept.dept_id, 'ushiga-a');
  assert.strictEqual(r1.excluded.byRegion, 1);
  assert.strictEqual(r1.excluded.unconfirmed, 1);
  // regions空 → 地域で切らない（unconfirmedのみ除外、通過2件）
  const r2 = E.hardFilter(departments, universities, { regions: [] });
  assert.strictEqual(r2.passed.length, 2);
  assert.strictEqual(r2.excluded.byRegion, 0);
  // commuteOnly → commute_possible!==true を除外
  const r3 = E.hardFilter(departments, universities, { regions: [], commuteOnly: true });
  assert.strictEqual(r3.passed.length, 1);
  assert.strictEqual(r3.passed[0].dept.dept_id, 'ushiga-a');
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `node test_engine_v2.js`
Expected: FAIL（`E.hardFilter is not a function`）

- [ ] **Step 3: engine_v2.js に hardFilter を追加（exportにも追加）**

```js
function hardFilter(departments, universities, userProfile) {
  const uniById = Object.fromEntries(universities.map((u) => [u.id, u]));
  const regions = new Set(userProfile.regions || []);
  const excluded = { byRegion: 0, unconfirmed: 0 };
  const passed = [];
  for (const d of departments) {
    const uni = uniById[d.uni_id];
    if (!uni) continue;
    if (d.data_status === 'unconfirmed') { excluded.unconfirmed++; continue; }
    if (regions.size && !regions.has(uni.region)) { excluded.byRegion++; continue; }
    if (userProfile.commuteOnly && uni.commute_possible !== true) { excluded.byRegion++; continue; }
    passed.push({ dept: d, uni });
  }
  return { passed, excluded };
}
```

exportに `hardFilter` を追加。

- [ ] **Step 4: テストが通ることを確認**

Run: `node test_engine_v2.js`
Expected: `ALL OK`

- [ ] **Step 5: コミット**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git add work/engine_v2.js work/test_engine_v2.js
git commit -m "feat(engine): hardFilter（地域・通学・unconfirmed除外）"
```

---

### Task 5: computeFit（適合スコア）

**Files:**
- Modify: `work/engine_v2.js`
- Modify: `work/test_engine_v2.js`

`computeFit(dept, uni, ctx)` を実装。内部ヘルパ `deptRiasecVector`・`riasecFitScore` も追加・export。

- [ ] **Step 1: 失敗するテストを追記（`console.log('ALL OK');` の直前）**

```js
// ---- deptRiasecVector / riasecFitScore ----
{
  const map = { keizai: { R: 0, I: 0.4, A: 0.1, S: 0.3, E: 0.8, C: 0.6 },
                joho:   { R: 0.4, I: 0.8, A: 0.2, S: 0.1, E: 0.3, C: 0.6 },
                unclassified: { R: 0, I: 0, A: 0, S: 0, E: 0, C: 0 } };
  // 単一タグ → そのまま
  const dv1 = E.deptRiasecVector({ interest_tags: ['keizai'] }, map);
  assert.strictEqual(dv1.E, 0.8);
  // 複数タグ → 平均
  const dv2 = E.deptRiasecVector({ interest_tags: ['keizai', 'joho'] }, map);
  assert.ok(Math.abs(dv2.I - 0.6) < 1e-9, 'I avg 0.6: ' + dv2.I);
  // タグ無し → unclassified扱いで全0
  const dv3 = E.deptRiasecVector({ interest_tags: [] }, map);
  assert.strictEqual(dv3.E, 0);
  // riasecFit: ユーザーがE型MAX、学部がkeizai → dot/sum
  const uv = { R: 0, I: 0, A: 0, S: 0, E: 1, C: 0 };
  const fit = E.riasecFitScore(uv, dv1); // 0.8 / (0+0.4+0.1+0.3+0.8+0.6)=0.8/2.2
  assert.ok(Math.abs(fit - 0.8 / 2.2) < 1e-9, 'riasecFit: ' + fit);
  // 全0学部 → 0
  assert.strictEqual(E.riasecFitScore(uv, dv3), 0);
}
// ---- computeFit ----
{
  const map = { keizai: { R: 0, I: 0.4, A: 0.1, S: 0.3, E: 0.8, C: 0.6 },
                unclassified: { R: 0, I: 0, A: 0, S: 0, E: 0, C: 0 } };
  const dept = {
    interest_tags: ['keizai'], employment_fields: ['kinyu', 'it'],
    entry_methods: [{ type: 'ippan', available: true }, { type: 'kyotsu', available: false }],
    zemi: [{ name: 'Aゼミ' }],
  };
  const uni = { prefecture: '滋賀県', commute_possible: true, tuition_yen_per_year: 1200000 };
  const ctx = {
    riasecProfile: { R: 0, I: 0, A: 0, S: 0, E: 1, C: 0 }, hollandCode: 'EC...',
    interests: new Set(['keizai']), careers: new Set(['kinyu']),
    examMethods: new Set(['ippan']), budgetMax: null, riasecMap: map,
  };
  const r = E.computeFit(dept, uni, ctx);
  // 適性: 40*(0.8/2.2)=14.5 + 15*1(興味一致)=15 → 29.5 / 就職: kinyu一致=8 / ソフト: ippan可+6, ゼミ+4, 滋賀+8 =18
  // 合計 55.5 前後
  assert.ok(r.fitScore > 50 && r.fitScore < 61, 'fitScore around 55: ' + r.fitScore);
  assert.strictEqual(r.breakdown.emp, 8);
  assert.ok(r.reasons.length > 0 && r.reasons.length <= 5);
  // 予算超過は減点
  const r2 = E.computeFit(dept, uni, { ...ctx, budgetMax: 1000000 });
  assert.ok(r2.fitScore < r.fitScore, '予算超過で減点');
  // 使える方式が噛み合わない → 減点
  const deptSogoOnly = { ...dept, entry_methods: [{ type: 'sogo', available: true }] };
  const r3 = E.computeFit(deptSogoOnly, uni, ctx);
  assert.ok(r3.breakdown.soft < r.breakdown.soft, '方式不一致で減点');
  // クランプ: 0未満にならない
  const bad = E.computeFit(deptSogoOnly, { prefecture: '東京都', tuition_yen_per_year: 5000000 },
    { riasecProfile: { R: 0, I: 0, A: 0, S: 0, E: 0, C: 0 }, hollandCode: '',
      interests: new Set(), careers: new Set(), examMethods: new Set(['ippan']), budgetMax: 1000000, riasecMap: map });
  assert.ok(bad.fitScore >= 0, 'クランプ下限0: ' + bad.fitScore);
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `node test_engine_v2.js`
Expected: FAIL（`E.deptRiasecVector is not a function`）

- [ ] **Step 3: engine_v2.js に3関数を追加（exportにも追加）**

```js
function deptRiasecVector(dept, riasecMap) {
  const tags = (dept.interest_tags && dept.interest_tags.length) ? dept.interest_tags : ['unclassified'];
  const acc = { R: 0, I: 0, A: 0, S: 0, E: 0, C: 0 };
  let n = 0;
  for (const tag of tags) {
    const w = riasecMap[tag];
    if (!w) continue;
    for (const t of RIASEC_TYPES) acc[t] += (w[t] || 0);
    n++;
  }
  if (n > 0) for (const t of RIASEC_TYPES) acc[t] /= n;
  return acc;
}

// uv: ユーザー6次元(0-1), dv: 学部6次元 → 学部側重み和で正規化した加重内積(0-1)
function riasecFitScore(uv, dv) {
  const sumDv = RIASEC_TYPES.reduce((s, t) => s + dv[t], 0);
  if (sumDv === 0) return 0;
  const dot = RIASEC_TYPES.reduce((s, t) => s + uv[t] * dv[t], 0);
  return dot / sumDv;
}

// ctx: { riasecProfile, hollandCode, interests:Set, careers:Set, examMethods:Set, budgetMax, riasecMap }
function computeFit(dept, uni, ctx) {
  const reasons = [];
  // ① 適性（最大55）
  const dv = deptRiasecVector(dept, ctx.riasecMap);
  const rf = riasecFitScore(ctx.riasecProfile, dv);
  const apptRiasec = 40 * rf;
  const tags = dept.interest_tags || [];
  const overlap = ctx.interests.size ? tags.filter((t) => ctx.interests.has(t)).length : 0;
  const interestRatio = ctx.interests.size ? Math.min(1, overlap / ctx.interests.size) : 0;
  const apptInterest = 15 * interestRatio;
  const appt = apptRiasec + apptInterest;
  // ② 就職（最大25）
  const empOverlap = (dept.employment_fields || []).filter((f) => ctx.careers.has(f)).length;
  const emp = Math.min(25, empOverlap * 8);
  // ③ ソフト（最大20・減点あり）
  let soft = 0;
  const availTypes = (dept.entry_methods || []).filter((m) => m.available === true).map((m) => m.type);
  const usable = availTypes.some((t) => ctx.examMethods.has(t));
  if (ctx.examMethods.size) {
    if (usable) soft += 6;
    else if (availTypes.length) soft -= 10;
  }
  if (ctx.budgetMax != null && uni.tuition_yen_per_year != null && uni.tuition_yen_per_year > ctx.budgetMax) soft -= 8;
  if ((dept.zemi || []).length > 0) soft += 4;
  const pref = uni.prefecture || '';
  if (pref.includes('滋賀')) soft += 8;
  else if (uni.commute_possible === true) soft += 5;
  // reasons
  if (apptRiasec >= 20) reasons.push(`🧭 適性◎ ${ctx.hollandCode || ''} と好相性`);
  else if (apptRiasec >= 10) reasons.push('🧭 適性○');
  if (apptInterest > 0) reasons.push('📚 興味分野に一致');
  if (emp > 0) reasons.push('💼 希望の就職系統に強い');
  if ((dept.zemi || []).length > 0) reasons.push('🎓 看板ゼミあり');
  if (pref.includes('滋賀')) reasons.push('🏞️ 滋賀県内・地元');
  else if (uni.commute_possible === true) reasons.push('🚃 通学圏');
  if (ctx.examMethods.size && !usable && availTypes.length) reasons.push('⚠️ 使える入試方式と噛み合わない');
  const fitScore = Math.max(0, Math.min(100, appt + emp + soft));
  return { fitScore: round1(fitScore), reasons: reasons.slice(0, 5), breakdown: { appt: round1(appt), emp, soft, riasecFit: round2(rf) } };
}
```

exportに `deptRiasecVector, riasecFitScore, computeFit` を追加。

- [ ] **Step 4: テストが通ることを確認**

Run: `node test_engine_v2.js`
Expected: `ALL OK`

- [ ] **Step 5: コミット**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git add work/engine_v2.js work/test_engine_v2.js
git commit -m "feat(engine): computeFit（適性RIASEC×興味＋就職＋ソフト条件）"
```

---

### Task 6: diagnose（統合）と実データスモーク

**Files:**
- Modify: `work/engine_v2.js`
- Modify: `work/test_engine_v2.js`

- [ ] **Step 1: 失敗するテストを追記（`console.log('ALL OK');` の直前）**

```js
// ---- diagnose 統合（合成フィクスチャ）----
{
  const riasecMap = require('./interest_riasec_map.json').map;
  const universities = [
    { id: 'u1', name: '滋賀A大', region: 'shiga', prefecture: '滋賀県', commute_possible: true, tuition_yen_per_year: 1200000 },
    { id: 'u2', name: '関東B大', region: 'kanto', prefecture: '東京都', commute_possible: false, tuition_yen_per_year: 1300000 },
  ];
  const mkDept = (id, uni, tags, hmin, hmax, status = 'verified') => ({
    dept_id: id, uni_id: uni, name: id + '学部', campus: 'C', data_status: status,
    hensachi: { min: hmin, max: hmax }, bairitsu: { ippan: {} },
    entry_methods: [{ type: 'ippan', available: true }], interest_tags: tags,
    employment_fields: ['kinyu'], zemi: [],
  });
  const departments = [
    mkDept('u1-keizai', 'u1', ['keizai'], 50, 52),   // mid51 gap+1 → 実力(換算50)
    mkDept('u1-joho', 'u1', ['joho'], 55, 57),       // mid56 gap+6 → 挑戦
    mkDept('u1-kyoiku', 'u1', ['kyoiku'], 44, 46),   // mid45 gap-5 → 安全
    mkDept('u2-keizai', 'u2', ['keizai'], 50, 52),   // 関東 → 地域除外
    mkDept('u1-x', 'u1', ['keizai'], 65, 67, 'unconfirmed'), // unconfirmed除外
  ];
  const dataV2 = { universities, departments };
  const profile = {
    hensachi: 50, examSource: 'kawai', regions: ['shiga'], commuteOnly: false,
    riasecAnswers: [0, 0, 3, 3, 0, 0, 0, 0, 3, 3, 0, 0], // I高E高
    interests: ['keizai'], careers: ['kinyu'], examMethods: ['ippan'], budgetMax: null,
  };
  const res = E.diagnose(profile, dataV2, null, riasecMap);
  assert.strictEqual(res.zones.match.length, 1);
  assert.strictEqual(res.zones.match[0].dept_id, 'u1-keizai');
  assert.strictEqual(res.zones.challenge[0].dept_id, 'u1-joho');
  assert.strictEqual(res.zones.safe[0].dept_id, 'u1-kyoiku');
  assert.strictEqual(res.excluded.byRegion, 1);
  assert.strictEqual(res.excluded.unconfirmed, 1);
  assert.strictEqual(res.meta.adjustedHensachi, 50);
  assert.ok(res.meta.hollandCode.length === 3);
  assert.strictEqual(res.meta.lowSignal, false);
  assert.ok(res.zones.match[0].fitScore > 0);
  // 偏差値未入力 → ungrouped に集約、zonesは空
  const res2 = E.diagnose({ ...profile, hensachi: null }, dataV2, null, riasecMap);
  assert.strictEqual(res2.zones.challenge.length, 0);
  assert.ok(res2.ungrouped.length >= 3);
  // lowSignal: RIASEC未回答＋興味なし
  const res3 = E.diagnose({ ...profile, riasecAnswers: [], interests: [] }, dataV2, null, riasecMap);
  assert.strictEqual(res3.meta.lowSignal, true);
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `node test_engine_v2.js`
Expected: FAIL（`E.diagnose is not a function`）

- [ ] **Step 3: engine_v2.js に diagnose を追加（exportにも追加）**

```js
// diagnose(userProfile, dataV2, tagsDef, riasecMap, opts?)
// tagsDef は将来の検証用に受けるが現状未使用（インターフェース安定のため引数に残す）
function diagnose(userProfile, dataV2, tagsDef, riasecMap, opts = {}) {
  const topN = opts.topN || DEFAULT_TOP_N;
  const { profile, hollandCode, answered } = computeRiasecProfile(userProfile.riasecAnswers);
  const interests = new Set(userProfile.interests || []);
  const careers = new Set(userProfile.careers || []);
  const examMethods = new Set(userProfile.examMethods || []);
  const lowSignal = !answered && interests.size === 0;
  const adjHen = adjustedHensachi(userProfile.hensachi, userProfile.examSource);

  const { passed, excluded } = hardFilter(dataV2.departments, dataV2.universities, userProfile);
  excluded.outOfRange = 0;

  const ctx = { riasecProfile: profile, hollandCode, interests, careers, examMethods, budgetMax: userProfile.budgetMax ?? null, riasecMap };
  const zones = { challenge: [], match: [], safe: [] };
  const ungrouped = [];

  for (const { dept, uni } of passed) {
    const min = dept.hensachi ? dept.hensachi.min : null;
    const max = dept.hensachi ? dept.hensachi.max : null;
    const mid = (min != null && max != null) ? (min + max) / 2 : (min != null ? min : (max != null ? max : null));
    const zone = classifyZone(mid, adjHen);
    if (zone === 'out') { excluded.outOfRange++; continue; }
    const fit = computeFit(dept, uni, ctx);
    const item = {
      dept_id: dept.dept_id, uni_id: uni.id, uni_name: uni.name, dept_name: dept.name,
      campus: dept.campus, hensachi: { min, max, mid },
      bairitsu: dept.bairitsu, zone: zone === 'ungrouped' ? null : zone,
      fitScore: fit.fitScore, reasons: fit.reasons, breakdown: fit.breakdown,
    };
    if (zone === 'ungrouped') ungrouped.push(item);
    else zones[zone].push(item);
  }

  const byFit = (a, b) => (b.fitScore - a.fitScore) || ((b.hensachi.mid || 0) - (a.hensachi.mid || 0));
  for (const k of ['challenge', 'match', 'safe']) zones[k].sort(byFit);
  ungrouped.sort(byFit);

  return {
    zones: { challenge: zones.challenge.slice(0, topN), match: zones.match.slice(0, topN), safe: zones.safe.slice(0, topN) },
    ungrouped: ungrouped.slice(0, topN * 3),
    excluded,
    meta: { adjustedHensachi: adjHen, riasecProfile: profile, hollandCode, lowSignal },
  };
}
```

exportに `diagnose` を追加。

- [ ] **Step 4: テストが通ることを確認**

Run: `node test_engine_v2.js`
Expected: `ALL OK`

- [ ] **Step 5: 実data_v2でスモーク実行（クラッシュ無し・ゾーン非空・地域除外を確認）**

Run:
```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版/work"
node -e "
const E = require('./engine_v2.js');
const data = require('./data_v2.json');
const map = require('./interest_riasec_map.json').map;
const profile = {
  hensachi: 52, examSource: 'kawai', regions: ['shiga','kyoto','osaka'], commuteOnly: false,
  riasecAnswers: [1,1,3,3,0,1,2,2,1,1,2,2], interests: ['keizai','joho'],
  careers: ['it','kinyu'], examMethods: ['ippan','kyotsu'], budgetMax: null,
};
const r = E.diagnose(profile, data, data.tags, map);
console.log('挑戦:', r.zones.challenge.length, '実力:', r.zones.match.length, '安全:', r.zones.safe.length);
console.log('除外:', JSON.stringify(r.excluded));
console.log('holland:', r.meta.hollandCode, 'lowSignal:', r.meta.lowSignal);
console.log('実力トップ3:', r.zones.match.slice(0,3).map(x => x.uni_name + x.dept_name + '(' + x.fitScore + ')'));
const region = E.diagnose({ ...profile, regions: ['shiga'] }, data, data.tags, map);
console.log('滋賀のみ excluded.byRegion:', region.excluded.byRegion, region.excluded.byRegion > 0 ? 'OK' : 'NG');
"
```
Expected: 3ゾーンとも件数が出て（0でないゾーンがある）、除外集計が妥当、`holland` が3文字、`滋賀のみ excluded.byRegion` が正の値で `OK`。異常（全ゾーン0、NaN、クラッシュ）があれば原因を調べて修正。

- [ ] **Step 6: コミット**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git add work/engine_v2.js work/test_engine_v2.js
git commit -m "feat(engine): diagnose統合＋実データスモーク（3ゾーン出力）"
```

---

### Task 7: 確認用ハーネス engine_harness.html

**Files:**
- Create: `work/engine_harness.html`

目視確認専用の使い捨てビューア。`data_v2.json`・`riasec_def.json`・`interest_riasec_map.json`・`engine_v2.js` を読み込み、最小フォームで `diagnose()` を叩いて3ゾーンを描画する。UIの作り込みはしない（本UIはフェーズ3）。

- [ ] **Step 1: engine_harness.html を作成**

engine_v2.js は `module.exports` を使うため、ブラウザで読めるよう末尾に `if (typeof window !== 'undefined') window.EngineV2 = { ... }` を足す方針は取らない（v2本体を汚さない）。代わりにハーネス側で `module` をshimして評価する。

```html
<!doctype html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>engine_v2 ハーネス（確認用）</title>
<style>
  body { font-family: system-ui, sans-serif; margin: 16px; max-width: 900px; }
  fieldset { margin-bottom: 12px; }
  .zone { margin-top: 16px; }
  .zone h2 { font-size: 15px; margin: 8px 0; }
  .item { border: 1px solid #ccc; border-radius: 6px; padding: 8px; margin: 6px 0; }
  .item .top { display: flex; justify-content: space-between; gap: 8px; }
  .fit { font-weight: 700; color: #2563eb; }
  .reasons { font-size: 12px; color: #555; margin-top: 4px; }
  .meta { background: #f3f4f6; padding: 8px; border-radius: 6px; font-size: 13px; }
  label { font-size: 13px; }
  .riasec-q { display: block; margin: 2px 0; font-size: 12px; }
  button { padding: 8px 16px; font-size: 14px; }
</style>
</head>
<body>
<h1>診断エンジンv2 確認ハーネス</h1>
<p style="color:#b91c1c;font-size:12px">※ 目視確認専用。本番UIはフェーズ3で作成。</p>

<fieldset><legend>偏差値・模試</legend>
  偏差値 <input id="hensachi" type="number" value="52" style="width:70px">
  模試 <select id="examSource"><option value="kawai">河合</option><option value="shinken">進研</option><option value="sundai">駿台</option><option value="toshin">東進</option></select>
  <label><input id="hensachiNull" type="checkbox"> 未入力</label>
</fieldset>
<fieldset><legend>エリア（ハードフィルタ）</legend><span id="regions"></span>
  <label><input id="commuteOnly" type="checkbox"> 通学可の大学のみ</label>
</fieldset>
<fieldset><legend>興味分野</legend><span id="interests"></span></fieldset>
<fieldset><legend>希望就職系統</legend><span id="careers"></span></fieldset>
<fieldset><legend>使える入試方式</legend>
  <label><input type="checkbox" class="em" value="sogo">総合型</label>
  <label><input type="checkbox" class="em" value="koubo">学校推薦</label>
  <label><input type="checkbox" class="em" value="ippan" checked>一般</label>
  <label><input type="checkbox" class="em" value="kyotsu" checked>共テ利用</label>
</fieldset>
<fieldset><legend>RIASEC（0〜3）</legend><div id="riasec"></div></fieldset>
<button id="run">診断する</button>
<div id="meta" class="meta"></div>
<div id="out"></div>

<script>
async function loadJSON(p) { return (await fetch(p)).json(); }
async function loadEngine() {
  const src = await (await fetch('./engine_v2.js')).text();
  const module = { exports: {} };
  new Function('module', 'exports', src)(module, module.exports);
  return module.exports;
}
let DATA, RIASEC_DEF, MAP, ENGINE;
function chip(container, id, value, label, name) {
  const l = document.createElement('label'); l.className = 'riasec-q';
  l.innerHTML = `<input type="checkbox" data-${name}="${value}"> ${label}`;
  container.appendChild(l);
}
(async function init() {
  DATA = await loadJSON('./data_v2.json');
  RIASEC_DEF = await loadJSON('./riasec_def.json');
  MAP = (await loadJSON('./interest_riasec_map.json')).map;
  ENGINE = await loadEngine();
  const regionIds = [...new Set(DATA.universities.map(u => u.region))];
  regionIds.forEach(r => chip(document.getElementById('regions'), r, r, r, 'region'));
  DATA.tags.interest.forEach(t => chip(document.getElementById('interests'), t.id, t.id, t.label, 'interest'));
  DATA.tags.employment.forEach(t => chip(document.getElementById('careers'), t.id, t.id, t.label, 'career'));
  RIASEC_DEF.items.forEach((it, i) => {
    const d = document.createElement('div'); d.className = 'riasec-q';
    d.innerHTML = `${it.text} <input type="number" min="0" max="3" value="1" data-riasec="${i}" style="width:48px">`;
    document.getElementById('riasec').appendChild(d);
  });
  document.querySelector('[data-region="shiga"]')?.click();
})();
function getChecked(attr) { return [...document.querySelectorAll(`[data-${attr}]`)].filter(e => e.checked).map(e => e.getAttribute(`data-${attr}`)); }
document.getElementById('run').addEventListener('click', () => {
  const answers = [...document.querySelectorAll('[data-riasec]')].map(e => Number(e.value) || 0);
  const profile = {
    hensachi: document.getElementById('hensachiNull').checked ? null : Number(document.getElementById('hensachi').value),
    examSource: document.getElementById('examSource').value,
    regions: getChecked('region'), commuteOnly: document.getElementById('commuteOnly').checked,
    riasecAnswers: answers, interests: getChecked('interest'), careers: getChecked('career'),
    examMethods: [...document.querySelectorAll('.em')].filter(e => e.checked).map(e => e.value), budgetMax: null,
  };
  const r = ENGINE.diagnose(profile, DATA, DATA.tags, MAP);
  document.getElementById('meta').textContent =
    `換算偏差値:${r.meta.adjustedHensachi} / ホランド:${r.meta.hollandCode} / lowSignal:${r.meta.lowSignal} / 除外:${JSON.stringify(r.excluded)}`;
  const render = (title, arr) => {
    if (!arr.length) return '';
    return `<div class="zone"><h2>${title}（${arr.length}）</h2>` + arr.map(x =>
      `<div class="item"><div class="top"><span>${x.uni_name} ${x.dept_name} <small>偏差${x.hensachi.min}-${x.hensachi.max}</small></span><span class="fit">${x.fitScore}</span></div><div class="reasons">${x.reasons.join(' ・ ')}</div></div>`).join('') + `</div>`;
  };
  const out = document.getElementById('out');
  if (r.ungrouped.length) out.innerHTML = render('適性順（偏差値未入力）', r.ungrouped);
  else out.innerHTML = render('🚀 挑戦', r.zones.challenge) + render('🎯 実力', r.zones.match) + render('🛡️ 安全', r.zones.safe);
});
</script>
</body>
</html>
```

- [ ] **Step 2: ブラウザで手動確認（プレビュー）**

`work/` を静的配信してブラウザで開き、エリア=滋賀+京都、偏差値52、RIASECを適当に入力→「診断する」で3ゾーンが描画され、除外集計・ホランドコードが表示されることを目視確認する（Browser preview toolがあれば `preview_start` で `work/engine_harness.html` を開く）。

Expected: エラーなく3ゾーンにカードが並ぶ。fit・reasons・偏差値帯が見える。異常があれば engine_v2.js ではなくハーネス側の配線を疑う（engine本体はTask6のスモークで検証済み）。

- [ ] **Step 3: コミット**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git add work/engine_harness.html
git commit -m "feat(engine): 確認用ハーネスHTML（3ゾーン目視）"
```

---

### Task 8: 最終レビューとフェーズ2完了

**Files:**
- なし（確認とサマリーのみ）

- [ ] **Step 1: 全テストとv1無変更を確認**

Run:
```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版/work"
node test_engine_v2.js
node check_v2.js
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git log --oneline --all -- 進路診断_一般版.html work/app.html | head -3
git status --porcelain
```
Expected: `ALL OK`（engineテスト）＋ `CHECK PASS`（data_v2は無変更なので従来通り verified=642）。v1ファイルへの新規変更コミットが無い。working treeがclean。

- [ ] **Step 2: フェーズ3への引き継ぎメモを追記**

`docs/superpowers/specs/2026-07-15-診断エンジンv2-design.md` の末尾「引き継ぎ」節に、実装後の実測（3ゾーンの典型件数、調整した重み値があればその旨）を1〜2行追記する。

- [ ] **Step 3: コミットとユーザー報告**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git add docs/ && git commit -m "docs: 診断エンジンv2 実装後の引き継ぎメモ"
```

ユーザーに報告：エンジンの公開API（`diagnose`）、3ゾーン出力の実測、フェーズ3（スマホUI）での組み込み方（このモジュールをimportしdiagnose結果を描画）。

---

## セルフレビュー結果

- **Spec coverage**: 純粋モジュール＝Task2-6 / 地域ハードフィルタ＝Task4 / RIASEC質問票＋算出＝Task1,2 / ゾーン分類＝Task3 / 適合スコア(55/25/20)＝Task5 / 14×6重み表＝Task1 / 出力契約＝Task6 / テスト全ケース＝各Task / ハーネス＝Task7 / v1・data_v2無変更＝Task8。全spec要件にタスクあり。
- **Placeholder scan**: 各コードステップは完全なコードを掲載。TBD/TODO無し。
- **型整合**: `diagnose(userProfile, dataV2, tagsDef, riasecMap, opts?)` の署名、item/zones/excluded/meta の形、関数名（computeRiasecProfile / adjustedHensachi / classifyZone / hardFilter / deptRiasecVector / riasecFitScore / computeFit / diagnose）を全タスクで統一。riasecMap は「map」プロパティ配下のオブジェクトを渡す（Task6スモーク・ハーネス・テストで一貫）。
- **注意点**: EXAM_SOURCE_OFFSET は Task3 Step1 で v1実値と突き合わせ、テスト期待値もそれに合わせる（specの-5/+2等は暫定既定値）。
