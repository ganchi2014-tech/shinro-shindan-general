# フェーズ1: 学部単位データ基盤 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1の大学単位データ（123校）を学部単位（約650学部）の2層構造 `data_v2.json` に再編し、Web調査で偏差値・倍率・入試方式を実データ検証（出典URL＋検証日付き）、全学部に看板ゼミを付属させる。

**Architecture:** `work/` に変換スクリプト（to_v2.js）・検収チェック（check_v2.js）・調査結果マージ（merge_research.js）を新設。Web調査はエージェント並列で1大学＝1 JSONファイル（`work/research/<uni_id>.json`）を生成し、merge_research.js で data_v2.json に取り込む。全差分は `docs/data-verification/` に記録。v1アプリ（進路診断_一般版.html / site/）には一切触れない。

**Tech Stack:** Node.js（既存 work/ と同じ、依存パッケージなし・assertによる素のテスト）。Web調査は Claude エージェント（WebSearch/WebFetch）。

**Spec:** `docs/superpowers/specs/2026-07-13-学部単位データ基盤-design.md`

**注意（環境）:**
- 作業ディレクトリ `C:\Users\togan1080\OneDrive\Claude\進路診断_一般版` は**まだgitリポジトリではない**（Task 0 で初期化）。`site/` は独立したgitリポジトリなので親リポからは除外する
- OneDrive配下のため、書き込み後は `ls` で実在確認する習慣を守る
- コマンド例はGit Bash（POSIX）表記。`cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版/work"` を起点とする

---

### Task 0: プロジェクトのgitリポジトリ化

**Files:**
- Create: `.gitignore`（プロジェクトルート）

- [ ] **Step 1: .gitignore を作成**

```gitignore
site/
進路診断_一般版.zip
node_modules/
```

（site/ は独立リポ、zipは生成物のため除外。ルートの 進路診断_一般版.html はv1配布正本なので追跡する）

- [ ] **Step 2: git init と初期コミット**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git init
git add -A
git commit -m "chore: v1完成時点のスナップショット（フェーズ1開始前）"
```

Expected: 初期コミット成功。`git status` がclean。

---

### Task 1: タグ定義表 tags_def.json

**Files:**
- Create: `work/tags_def.json`

- [ ] **Step 1: tags_def.json を作成**

interest は v1 の `INTEREST_OPTIONS`（app.html 1906行〜）の14分野をIDだけASCII正規化して継承（暫定タクソノミー。フェーズ2で確定後リタグ）。employment は新設12系統。

```json
{
  "version": "v2-provisional-2026-07",
  "note": "interestはフェーズ2の適性診断設計で確定後に一括リタグする暫定版。IDは変更禁止（リタグはmapping tableで行う）",
  "interest": [
    { "id": "bungaku",   "label": "文学・歴史・哲学",        "dept_keywords": ["文学", "文化", "歴史", "人文"] },
    { "id": "gaikokugo", "label": "外国語・国際",            "dept_keywords": ["外国語", "国際", "英文", "グローバル"] },
    { "id": "hokei",     "label": "法律・政治",              "dept_keywords": ["法", "政治", "政策"] },
    { "id": "keizai",    "label": "経済・経営・商学",        "dept_keywords": ["経済", "経営", "商", "ビジネス", "政治経済"] },
    { "id": "shakai",    "label": "社会・メディア・心理",    "dept_keywords": ["社会", "メディア", "心理", "コミュニケーション", "現代社会"] },
    { "id": "kyoiku",    "label": "教育・こども",            "dept_keywords": ["教育", "児童", "保育", "こども", "子ども", "発達"] },
    { "id": "riko",      "label": "理学・工学（ものづくり）", "dept_keywords": ["理", "工", "機械", "電気", "電子", "材料", "物質", "基礎工"] },
    { "id": "joho",      "label": "情報・データサイエンス",  "dept_keywords": ["情報", "データサイエンス", "システム", "コンピュータ"] },
    { "id": "kenchiku",  "label": "建築・デザイン",          "dept_keywords": ["建築", "デザイン", "住環境", "造形"] },
    { "id": "nogaku",    "label": "農学・食・環境",          "dept_keywords": ["農", "食", "環境", "バイオ", "生命", "和食"] },
    { "id": "iryo",      "label": "医療・看護・薬学",        "dept_keywords": ["医", "看護", "薬", "保健", "リハビリ", "理学療法", "作業療法", "歯"] },
    { "id": "eiyo",      "label": "栄養・家政・生活",        "dept_keywords": ["栄養", "家政", "生活", "食物"] },
    { "id": "sports",    "label": "スポーツ・健康",          "dept_keywords": ["スポーツ", "体育", "健康"] },
    { "id": "geijutsu",  "label": "芸術・音楽",              "dept_keywords": ["芸術", "音楽", "美術", "表象"] }
  ],
  "employment": [
    { "id": "koumuin",   "label": "公務員・行政" },
    { "id": "kyoiku",    "label": "教育（教員・保育）" },
    { "id": "kinyu",     "label": "金融・保険" },
    { "id": "maker",     "label": "メーカー・製造" },
    { "id": "it",        "label": "IT・通信" },
    { "id": "ryutsu",    "label": "商社・流通・小売" },
    { "id": "iryo_fukushi", "label": "医療・福祉" },
    { "id": "kensetsu",  "label": "建設・不動産" },
    { "id": "media",     "label": "マスコミ・広告" },
    { "id": "service",   "label": "サービス・観光" },
    { "id": "shokuhin",  "label": "食品・農林" },
    { "id": "sports_kenko", "label": "スポーツ・健康産業" }
  ]
}
```

- [ ] **Step 2: JSONとして妥当か確認**

```bash
node -e "const t=require('./tags_def.json'); console.log('interest:', t.interest.length, 'employment:', t.employment.length)"
```

Expected: `interest: 14 employment: 12`

- [ ] **Step 3: コミット**

```bash
git add work/tags_def.json && git commit -m "feat: タグ定義表（interest14暫定+employment12）"
```

---

### Task 2: 変換スクリプト to_v2.js（v1→v2骨格）

**Files:**
- Create: `work/to_v2.js`
- Test: `work/test_to_v2.js`

**入力**: `data_final.json`（5カテゴリ: base/sogo/koubo/ippan/employment、大学123校、ippanに650学部）
**出力**: `data_v2.json`（2層構造）＋ `dept_manifest.json`（全dept_id一覧＝調査の台帳）

- [ ] **Step 1: 失敗するテストを書く（test_to_v2.js）**

```js
// work/test_to_v2.js — node test_to_v2.js で実行
const assert = require('assert');
const { slugify, autoTags, buildDepartments, buildUniversity } = require('./to_v2.js');

const tagsDef = require('./tags_def.json');

// --- slugify: マップにある学部名 ---
{
  const used = new Set();
  assert.strictEqual(slugify('経営学部', used), 'keiei');
  assert.strictEqual(slugify('経営学部', used), 'keiei2'); // 重複時は連番
}
// --- slugify: マップにない学部名はフォールバック連番 ---
{
  const used = new Set();
  assert.strictEqual(slugify('グローバル・コミュニケーション学部', used), 'd01');
  assert.strictEqual(slugify('未知の学部', used), 'd02');
}
// --- autoTags: キーワードマッチ ---
{
  const t1 = autoTags('経営学部', tagsDef);
  assert.ok(t1.includes('keizai'), 'keizai expected: ' + t1);
  const t2 = autoTags('謎学部', tagsDef);
  assert.deepStrictEqual(t2, ['unclassified']);
}
// --- buildDepartments: 1大学分の学部レコード生成 ---
{
  const ippanUni = {
    id: 'testuni', name: 'テスト大学',
    departments: [
      { name: '経営学部', campus: '本キャンパス', hensachi_min: 50, hensachi_max: 55, qualifications: ['簿記'] },
      { name: '経営学部', campus: '第2', hensachi_min: 45, hensachi_max: 47.5, qualifications: [] }
    ]
  };
  const catAvail = { sogo: true, koubo: false, kokkoritsu: false };
  const depts = buildDepartments(ippanUni, catAvail, tagsDef);
  assert.strictEqual(depts.length, 2);
  assert.strictEqual(depts[0].dept_id, 'testuni-keiei');
  assert.strictEqual(depts[1].dept_id, 'testuni-keiei2');
  assert.strictEqual(depts[0].hensachi.min, 50);
  assert.strictEqual(depts[0].hensachi.basis, 'kawai');
  assert.strictEqual(depts[0].hensachi.source_url, null);
  assert.strictEqual(depts[0].data_status, 'estimated');
  assert.deepStrictEqual(depts[0].zemi, []);
  const sogo = depts[0].entry_methods.find(m => m.type === 'sogo');
  const koubo = depts[0].entry_methods.find(m => m.type === 'koubo');
  assert.strictEqual(sogo.available, true);
  assert.strictEqual(koubo.available, false);
}
// --- buildUniversity: base+employment統合、departmentsは持たない ---
{
  const base = { id: 'testuni', name: 'テスト大学', type: '私立', region: 'shiga', zemi_features: 'ゼミ紹介文' };
  const emp = { id: 'testuni', ratings: { kyoin: 3 }, strengths: ['a'], representative_employers: ['X社'], summary: 's' };
  const detail = { sogo: [{ method_name: 'AO' }], koubo: [], ippan: [] };
  const u = buildUniversity(base, emp, detail);
  assert.strictEqual(u.id, 'testuni');
  assert.strictEqual(u.zemi_features, 'ゼミ紹介文');       // フォールバック用に残す
  assert.strictEqual(u.employment.summary, 's');
  assert.strictEqual(u.entry_method_details.sogo[0].method_name, 'AO');
  assert.strictEqual(u.departments, undefined);
}
console.log('ALL OK');
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
node test_to_v2.js
```

Expected: FAIL（`Cannot find module './to_v2.js'`）

- [ ] **Step 3: to_v2.js を実装**

```js
#!/usr/bin/env node
// v1 data_final.json（5カテゴリ）→ v2 2層構造（universities + departments）骨格変換
// 使い方: node to_v2.js  → data_v2.json / dept_manifest.json を生成
const fs = require('fs');
const path = require('path');

// 学部名→スラッグ（頻出のみ。無ければ d01, d02... フォールバック）
const SLUG_MAP = {
  '文学部': 'bungaku', '人文学部': 'jinbun', '人間学部': 'ningen', '人間科学部': 'ningenkagaku',
  '経済学部': 'keizai', '経営学部': 'keiei', '商学部': 'sho', '経済経営学部': 'keizaikeiei',
  '法学部': 'ho', '政策科学部': 'seisaku', '総合政策学部': 'sogoseisaku', '政治経済学部': 'seikei',
  '社会学部': 'shakai', '社会科学部': 'shakaikagaku', '現代社会学部': 'gendaishakai',
  '国際学部': 'kokusai', '国際関係学部': 'kokusaikankei', '外国語学部': 'gaikokugo', '国際教養学部': 'kokusaikyoyo',
  '教育学部': 'kyoiku', '教育学域': 'kyoiku', '発達教育学部': 'hattatsukyoiku', 'こども教育学部': 'kodomokyoiku',
  '理学部': 'ri', '工学部': 'ko', '理工学部': 'riko', '基礎工学部': 'kisoko', '先進工学部': 'senshinko',
  '情報学部': 'joho', '情報理工学部': 'johoriko', 'データサイエンス学部': 'datascience', '総合情報学部': 'sogojoho',
  '建築学部': 'kenchiku', 'デザイン工学部': 'designko',
  '農学部': 'no', '生命科学部': 'seimei', '生命環境学部': 'seimeikankyou', '環境学部': 'kankyo',
  '医学部': 'i', '歯学部': 'shi', '薬学部': 'yaku', '看護学部': 'kango', '保健医療学部': 'hokeniryo',
  'リハビリテーション学部': 'reha', '健康科学部': 'kenkokagaku',
  '栄養学部': 'eiyo', '家政学部': 'kasei', '生活科学部': 'seikatsukagaku', '食物栄養科学部': 'shokumotsueiyo',
  'スポーツ健康科学部': 'sportskenko', 'スポーツ科学部': 'sportskagaku', '体育学部': 'taiiku', '健康スポーツ科学部': 'kenkosports',
  '芸術学部': 'geijutsu', '音楽学部': 'ongaku', '文芸学部': 'bungei',
  '心理学部': 'shinri', '総合心理学部': 'sogoshinri', '福祉学部': 'fukushi', '社会福祉学部': 'shakaifukushi',
  '産業社会学部': 'sanshakai', '観光学部': 'kanko', 'グローバル地域文化学部': 'globalchiiki',
};

function slugify(deptName, used) {
  let base = SLUG_MAP[deptName];
  if (!base) {
    // フォールバック: d01, d02...（この大学内で未使用の最小番号）
    let i = 1;
    while (used.has('d' + String(i).padStart(2, '0'))) i++;
    base = 'd' + String(i).padStart(2, '0');
    used.add(base);
    return base;
  }
  if (!used.has(base)) { used.add(base); return base; }
  let n = 2;
  while (used.has(base + n)) n++;
  used.add(base + n);
  return base + n;
}

function autoTags(deptName, tagsDef) {
  const tags = tagsDef.interest
    .filter(t => t.dept_keywords.some(k => deptName.includes(k)))
    .map(t => t.id);
  return tags.length ? tags : ['unclassified'];
}

// catAvail: { sogo: bool, koubo: bool, kokkoritsu: bool }（大学単位カテゴリの有無から初期値を推定）
function buildDepartments(ippanUni, catAvail, tagsDef) {
  const used = new Set();
  return (ippanUni.departments || []).map(d => {
    const slug = slugify(d.name, used);
    return {
      dept_id: `${ippanUni.id}-${slug}`,
      uni_id: ippanUni.id,
      name: d.name,
      campus: d.campus || null,
      hensachi: {
        min: d.hensachi_min ?? null,
        max: d.hensachi_max ?? null,
        basis: 'kawai',
        source_url: null,
        verified_date: null,
      },
      bairitsu: { ippan: {}, source_url: null, verified_date: null },
      entry_methods: [
        { type: 'sogo',   available: catAvail.sogo,        note: null, verified: false },
        { type: 'koubo',  available: catAvail.koubo,       note: null, verified: false },
        { type: 'ippan',  available: true,                 note: null, verified: false },
        { type: 'kyotsu', available: catAvail.kokkoritsu ? true : null, note: catAvail.kokkoritsu ? null : '共テ利用の有無は要調査', verified: false },
      ],
      qualifications: d.qualifications || [],
      interest_tags: autoTags(d.name, tagsDef),
      employment_fields: [],
      zemi: [],
      data_status: 'estimated',
    };
  });
}

function buildUniversity(baseUni, empUni, entryMethodDetails) {
  const u = { ...baseUni };
  delete u.departments; // 学部は第2層へ
  u.employment = empUni
    ? { ratings: empUni.ratings, strengths: empUni.strengths, representative_employers: empUni.representative_employers, summary: empUni.summary, notes: empUni.notes ?? null }
    : null;
  u.entry_method_details = entryMethodDetails; // v1の大学単位入試詳細（評定基準等）を温存
  return u;
}

function main() {
  const WORK = __dirname;
  const data = JSON.parse(fs.readFileSync(path.join(WORK, 'data_final.json'), 'utf8'));
  const tagsDef = JSON.parse(fs.readFileSync(path.join(WORK, 'tags_def.json'), 'utf8'));

  const byId = (cat) => Object.fromEntries((data[cat].universities || []).map(u => [u.id, u]));
  const sogoBy = byId('sogo'), kouboBy = byId('koubo'), ippanBy = byId('ippan'), empBy = byId('employment');

  const universities = [];
  const departments = [];
  for (const baseUni of data.base.universities) {
    const id = baseUni.id;
    const detail = {
      sogo: sogoBy[id]?.entry_methods || [],
      koubo: kouboBy[id]?.entry_methods || [],
      ippan: ippanBy[id]?.entry_methods || [],
    };
    universities.push(buildUniversity(baseUni, empBy[id] || null, detail));
    const catAvail = {
      sogo: (detail.sogo.length > 0),
      koubo: (detail.koubo.length > 0),
      kokkoritsu: (baseUni.type || '').includes('国立') || (baseUni.type || '').includes('公立') || (baseUni.type || '') === '国公立',
    };
    if (ippanBy[id]) departments.push(...buildDepartments(ippanBy[id], catAvail, tagsDef));
  }

  const out = {
    schema: 'shinro-general-v2',
    updated: new Date().toISOString().slice(0, 10),
    regions: data.base.regions,
    tags: tagsDef,
    universities,
    departments,
  };
  fs.writeFileSync(path.join(WORK, 'data_v2.json'), JSON.stringify(out, null, 1), 'utf8');

  const manifest = departments.map(d => ({ dept_id: d.dept_id, uni_id: d.uni_id, uni_name: universities.find(u => u.id === d.uni_id).name, dept_name: d.name }));
  fs.writeFileSync(path.join(WORK, 'dept_manifest.json'), JSON.stringify(manifest, null, 1), 'utf8');

  console.log(`universities: ${universities.length} / departments: ${departments.length}`);
  console.log('wrote data_v2.json, dept_manifest.json');
}

module.exports = { slugify, autoTags, buildDepartments, buildUniversity };
if (require.main === module) main();
```

- [ ] **Step 4: テストが通ることを確認**

```bash
node test_to_v2.js
```

Expected: `ALL OK`

- [ ] **Step 5: 実データで実行し、規模を確認**

```bash
node to_v2.js
```

Expected: `universities: 123 / departments: 650前後`（±10は許容。0や大幅な欠落はNG）

- [ ] **Step 6: スポットチェック**

```bash
node -e "
const d = require('./data_v2.json');
const rits = d.departments.filter(x => x.uni_id === 'ritsumeikan');
console.log('立命館の学部数:', rits.length);
console.log(JSON.stringify(rits.find(x => x.dept_id === 'ritsumeikan-keiei'), null, 1));
const un = d.departments.filter(x => x.interest_tags.includes('unclassified'));
console.log('unclassified学部数:', un.length, un.slice(0,10).map(x=>x.name));
"
```

Expected: 立命館に経営学部レコードあり・タグ付き。unclassified は少数（多ければSLUG_MAP/keywordsを補強してよいが、タグ確定はフェーズ2なので深追いしない）。

- [ ] **Step 7: コミット**

```bash
git add work/to_v2.js work/test_to_v2.js work/data_v2.json work/dept_manifest.json
git commit -m "feat: v1→v2骨格変換（大学123+学部650の2層構造）"
```

---

### Task 3: 検収チェックスクリプト check_v2.js

**Files:**
- Create: `work/check_v2.js`
- Test: `work/test_check_v2.js`

**役割**: specの検収基準を機械チェック。①必須フィールド網羅 ②dept_id一意 ③uni_id参照整合 ④verified値には source_url+verified_date 必須 ⑤ステータス別集計とゼミカバー率の表示。エラーがあれば exit 1。

- [ ] **Step 1: 失敗するテストを書く（test_check_v2.js）**

```js
// work/test_check_v2.js — node test_check_v2.js で実行
const assert = require('assert');
const { validate } = require('./check_v2.js');

function makeDept(over = {}) {
  return {
    dept_id: 'u1-keiei', uni_id: 'u1', name: '経営学部', campus: 'C',
    hensachi: { min: 50, max: 55, basis: 'kawai', source_url: null, verified_date: null },
    bairitsu: { ippan: {}, source_url: null, verified_date: null },
    entry_methods: [{ type: 'ippan', available: true, note: null, verified: false }],
    qualifications: [], interest_tags: ['keizai'], employment_fields: [], zemi: [],
    data_status: 'estimated',
    ...over,
  };
}
const baseData = { universities: [{ id: 'u1', name: 'テスト大学' }], departments: [makeDept()] };

// 正常系: エラーなし
{
  const r = validate(baseData);
  assert.deepStrictEqual(r.errors, []);
  assert.strictEqual(r.stats.total, 1);
  assert.strictEqual(r.stats.byStatus.estimated, 1);
}
// dept_id重複
{
  const r = validate({ ...baseData, departments: [makeDept(), makeDept()] });
  assert.ok(r.errors.some(e => e.includes('重複')));
}
// uni_id参照切れ
{
  const r = validate({ ...baseData, departments: [makeDept({ uni_id: 'nai' , dept_id: 'nai-x'})] });
  assert.ok(r.errors.some(e => e.includes('存在しない大学')));
}
// verifiedなのに出典なし
{
  const r = validate({ ...baseData, departments: [makeDept({ data_status: 'verified' })] });
  assert.ok(r.errors.some(e => e.includes('source_url')));
}
// verified + 出典あり → OK、ゼミカバー率
{
  const d = makeDept({
    data_status: 'verified',
    hensachi: { min: 50, max: 55, basis: 'kawai', source_url: 'https://x', verified_date: '2026-07-14' },
    zemi: [{ name: 'テストゼミ', theme: 't', source_url: 'https://x', verified_date: '2026-07-14' }],
  });
  const r = validate({ ...baseData, departments: [d] });
  assert.deepStrictEqual(r.errors, []);
  assert.strictEqual(r.stats.zemiCoverage, 1);
}
// 不正ステータス
{
  const r = validate({ ...baseData, departments: [makeDept({ data_status: '要確認' })] });
  assert.ok(r.errors.some(e => e.includes('data_status')));
}
console.log('ALL OK');
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
node test_check_v2.js
```

Expected: FAIL（`Cannot find module './check_v2.js'`）

- [ ] **Step 3: check_v2.js を実装**

```js
#!/usr/bin/env node
// data_v2.json の検収チェック。使い方: node check_v2.js
const fs = require('fs');
const path = require('path');

const REQUIRED = ['dept_id', 'uni_id', 'name', 'campus', 'hensachi', 'bairitsu', 'entry_methods', 'qualifications', 'interest_tags', 'employment_fields', 'zemi', 'data_status'];
const STATUSES = ['verified', 'estimated', 'unconfirmed'];

function validate(data) {
  const errors = [];
  const uniIds = new Set(data.universities.map(u => u.id));
  const seen = new Set();
  const byStatus = { verified: 0, estimated: 0, unconfirmed: 0 };
  let zemiCount = 0;

  for (const d of data.departments) {
    const label = d.dept_id || d.name || '(不明)';
    for (const f of REQUIRED) {
      if (!(f in d)) errors.push(`${label}: 必須フィールド ${f} がない`);
    }
    if (seen.has(d.dept_id)) errors.push(`${d.dept_id}: dept_id重複`);
    seen.add(d.dept_id);
    if (!uniIds.has(d.uni_id)) errors.push(`${label}: 存在しない大学を参照 (${d.uni_id})`);
    if (!STATUSES.includes(d.data_status)) errors.push(`${label}: data_status が不正 (${d.data_status})`);
    else byStatus[d.data_status]++;
    if (d.data_status === 'verified') {
      if (!d.hensachi?.source_url || !d.hensachi?.verified_date) {
        errors.push(`${label}: verified なのに hensachi の source_url / verified_date がない`);
      }
    }
    // 出典と検証日は必ずペア
    for (const [fname, obj] of [['hensachi', d.hensachi], ['bairitsu', d.bairitsu]]) {
      if (obj && ((obj.source_url && !obj.verified_date) || (!obj.source_url && obj.verified_date))) {
        errors.push(`${label}: ${fname} の source_url と verified_date がペアになっていない`);
      }
    }
    for (const z of d.zemi || []) {
      if (!z.name || !z.source_url || !z.verified_date) errors.push(`${label}: zemi に name/source_url/verified_date が欠落`);
    }
    if ((d.zemi || []).length > 0) zemiCount++;
    if ((d.zemi || []).length > 3) errors.push(`${label}: zemi が3件を超えている`);
  }
  const total = data.departments.length;
  return { errors, stats: { total, byStatus, zemiCount, zemiCoverage: total ? zemiCount / total : 0 } };
}

function main() {
  const data = JSON.parse(fs.readFileSync(path.join(__dirname, 'data_v2.json'), 'utf8'));
  const { errors, stats } = validate(data);
  console.log(`学部総数: ${stats.total}`);
  console.log(`ステータス: verified=${stats.byStatus.verified} estimated=${stats.byStatus.estimated} unconfirmed=${stats.byStatus.unconfirmed}`);
  console.log(`ゼミカバー率: ${(stats.zemiCoverage * 100).toFixed(1)}% (${stats.zemiCount}/${stats.total})`);
  if (errors.length) {
    console.error(`\nエラー ${errors.length}件:`);
    for (const e of errors.slice(0, 50)) console.error(' -', e);
    if (errors.length > 50) console.error(` ... 他${errors.length - 50}件`);
    process.exit(1);
  }
  console.log('\nCHECK PASS');
}

module.exports = { validate };
if (require.main === module) main();
```

- [ ] **Step 4: テストが通ることを確認**

```bash
node test_check_v2.js
```

Expected: `ALL OK`

- [ ] **Step 5: 実データに対して実行**

```bash
node check_v2.js
```

Expected: `CHECK PASS`（全650学部 estimated、ゼミカバー率0%の初期状態）

- [ ] **Step 6: コミット**

```bash
git add work/check_v2.js work/test_check_v2.js
git commit -m "feat: v2データ検収チェックスクリプト"
```

---

### Task 4: 調査結果マージスクリプト merge_research.js

**Files:**
- Create: `work/merge_research.js`
- Test: `work/test_merge_research.js`
- Create: `docs/data-verification/`（ディレクトリ、マージ実行時に自動生成）

**役割**: エージェント調査の成果物 `work/research/<uni_id>.json` を data_v2.json に取り込み、変更差分を `docs/data-verification/diff_log.jsonl`（機械可読）と `docs/data-verification/<uni_id>.md`（人間可読レポート）に記録する。

**調査結果JSONのスキーマ（エージェントが出力する形式）:**

```jsonc
{
  "uni_id": "ritsumeikan",
  "researched_date": "2026-07-14",
  "sources": ["https://www.keinet.ne.jp/...", "https://ritsnet.ritsumei.ac.jp/..."],
  "departments": [
    {
      "dept_id": "ritsumeikan-keiei",        // dept_manifest.json のIDと一致必須
      "hensachi": { "min": 57.5, "max": 60.0, "source_url": "https://..." },   // 省略可
      "bairitsu": { "ippan": { "2025": 3.4, "2026": 3.1 }, "source_url": "https://..." }, // 省略可
      "entry_methods": [                      // 省略可（判明した方式のみ）
        { "type": "kyotsu", "available": true, "note": "共通テスト利用方式あり" }
      ],
      "qualifications": ["公認会計士", "税理士"],  // 省略可（置き換え）
      "employment_fields": ["kinyu", "maker"],     // 省略可（tags_def.employment のID）
      "zemi": [ { "name": "○○ゼミ", "theme": "テーマ1行", "source_url": "https://..." } ], // 省略可・最大3件
      "unconfirmed": false                    // trueなら data_status=unconfirmed のまま
    }
  ],
  "notes": "調査時の特記事項"
}
```

**マージ規則**: 調査JSONに存在するフィールドだけ上書き。source_url を持つ値には verified_date=researched_date を刻印。hensachi に source_url が付けば data_status=verified（unconfirmed:true 指定時を除く）。dept_id がマニフェストに無ければエラー（黙って捨てない）。

- [ ] **Step 1: 失敗するテストを書く（test_merge_research.js）**

```js
// work/test_merge_research.js — node test_merge_research.js で実行
const assert = require('assert');
const { mergeUni } = require('./merge_research.js');

function makeDept(over = {}) {
  return {
    dept_id: 'u1-keiei', uni_id: 'u1', name: '経営学部', campus: 'C',
    hensachi: { min: 50, max: 55, basis: 'kawai', source_url: null, verified_date: null },
    bairitsu: { ippan: {}, source_url: null, verified_date: null },
    entry_methods: [
      { type: 'ippan', available: true, note: null, verified: false },
      { type: 'kyotsu', available: null, note: '共テ利用の有無は要調査', verified: false },
    ],
    qualifications: ['簿記'], interest_tags: ['keizai'], employment_fields: [], zemi: [],
    data_status: 'estimated',
    ...over,
  };
}

// 正常マージ: 偏差値更新→verified化、差分記録、ゼミ・共テ反映
{
  const data = { universities: [{ id: 'u1', name: 'テスト大学' }], departments: [makeDept()] };
  const research = {
    uni_id: 'u1', researched_date: '2026-07-14',
    departments: [{
      dept_id: 'u1-keiei',
      hensachi: { min: 52.5, max: 55, source_url: 'https://kei-net.example' },
      bairitsu: { ippan: { '2026': 3.2 }, source_url: 'https://uni.example' },
      entry_methods: [{ type: 'kyotsu', available: true, note: '共テ利用あり' }],
      employment_fields: ['kinyu'],
      zemi: [{ name: 'Aゼミ', theme: 'マーケティング', source_url: 'https://uni.example/zemi' }],
    }],
  };
  const diffs = mergeUni(data, research);
  const d = data.departments[0];
  assert.strictEqual(d.hensachi.min, 52.5);
  assert.strictEqual(d.hensachi.verified_date, '2026-07-14');
  assert.strictEqual(d.data_status, 'verified');
  assert.strictEqual(d.bairitsu.ippan['2026'], 3.2);
  const ky = d.entry_methods.find(m => m.type === 'kyotsu');
  assert.strictEqual(ky.available, true);
  assert.strictEqual(ky.verified, true);
  assert.strictEqual(d.zemi.length, 1);
  assert.strictEqual(d.zemi[0].verified_date, '2026-07-14');
  assert.deepStrictEqual(d.employment_fields, ['kinyu']);
  assert.strictEqual(d.qualifications[0], '簿記'); // 未提供フィールドは不変
  assert.ok(diffs.some(x => x.field === 'hensachi.min' && x.old === 50 && x.new === 52.5));
}
// unconfirmed指定: 上書きしてもステータスはunconfirmed
{
  const data = { universities: [{ id: 'u1', name: 'テスト大学' }], departments: [makeDept()] };
  const research = {
    uni_id: 'u1', researched_date: '2026-07-14',
    departments: [{ dept_id: 'u1-keiei', unconfirmed: true }],
  };
  mergeUni(data, research);
  assert.strictEqual(data.departments[0].data_status, 'unconfirmed');
}
// 不明なdept_id → throw
{
  const data = { universities: [{ id: 'u1', name: 'テスト大学' }], departments: [makeDept()] };
  const research = { uni_id: 'u1', researched_date: '2026-07-14', departments: [{ dept_id: 'u1-nai' }] };
  assert.throws(() => mergeUni(data, research), /u1-nai/);
}
// ゼミ4件以上 → throw
{
  const data = { universities: [{ id: 'u1', name: 'テスト大学' }], departments: [makeDept()] };
  const z = { name: 'x', theme: 't', source_url: 'https://x' };
  const research = { uni_id: 'u1', researched_date: '2026-07-14', departments: [{ dept_id: 'u1-keiei', zemi: [z, z, z, z] }] };
  assert.throws(() => mergeUni(data, research), /3件/);
}
console.log('ALL OK');
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
node test_merge_research.js
```

Expected: FAIL（`Cannot find module './merge_research.js'`）

- [ ] **Step 3: merge_research.js を実装**

```js
#!/usr/bin/env node
// 調査結果JSONを data_v2.json にマージし、差分を docs/data-verification/ に記録
// 使い方: node merge_research.js research/ritsumeikan.json [research/xxx.json ...]
const fs = require('fs');
const path = require('path');

const WORK = __dirname;
const VERIF_DIR = path.join(WORK, '..', 'docs', 'data-verification');

function setAndDiff(diffs, deptId, obj, field, newVal, pathLabel) {
  const oldVal = obj[field];
  if (JSON.stringify(oldVal) === JSON.stringify(newVal)) return;
  diffs.push({ dept_id: deptId, field: pathLabel, old: oldVal ?? null, new: newVal });
  obj[field] = newVal;
}

// data を破壊的に更新し、差分配列を返す
function mergeUni(data, research) {
  const date = research.researched_date;
  if (!date) throw new Error(`${research.uni_id}: researched_date がない`);
  const diffs = [];
  const byId = Object.fromEntries(data.departments.map(d => [d.dept_id, d]));

  for (const r of research.departments || []) {
    const d = byId[r.dept_id];
    if (!d) throw new Error(`不明な dept_id: ${r.dept_id}（dept_manifest.json を確認）`);

    if (r.hensachi) {
      setAndDiff(diffs, d.dept_id, d.hensachi, 'min', r.hensachi.min ?? d.hensachi.min, 'hensachi.min');
      setAndDiff(diffs, d.dept_id, d.hensachi, 'max', r.hensachi.max ?? d.hensachi.max, 'hensachi.max');
      if (r.hensachi.source_url) {
        d.hensachi.source_url = r.hensachi.source_url;
        d.hensachi.verified_date = date;
      }
    }
    if (r.bairitsu) {
      setAndDiff(diffs, d.dept_id, d.bairitsu, 'ippan', { ...d.bairitsu.ippan, ...r.bairitsu.ippan }, 'bairitsu.ippan');
      if (r.bairitsu.source_url) {
        d.bairitsu.source_url = r.bairitsu.source_url;
        d.bairitsu.verified_date = date;
      }
    }
    for (const m of r.entry_methods || []) {
      const target = d.entry_methods.find(x => x.type === m.type);
      if (!target) throw new Error(`${r.dept_id}: 不明な entry_method type ${m.type}`);
      setAndDiff(diffs, d.dept_id, target, 'available', m.available, `entry_methods.${m.type}.available`);
      if (m.note !== undefined) target.note = m.note;
      target.verified = true;
    }
    if (r.qualifications) setAndDiff(diffs, d.dept_id, d, 'qualifications', r.qualifications, 'qualifications');
    if (r.employment_fields) setAndDiff(diffs, d.dept_id, d, 'employment_fields', r.employment_fields, 'employment_fields');
    if (r.zemi) {
      if (r.zemi.length > 3) throw new Error(`${r.dept_id}: zemi は最大3件（${r.zemi.length}件指定）`);
      const zemi = r.zemi.map(z => ({ name: z.name, theme: z.theme || null, source_url: z.source_url, verified_date: date }));
      setAndDiff(diffs, d.dept_id, d, 'zemi', zemi, 'zemi');
    }
    if (r.unconfirmed) d.data_status = 'unconfirmed';
    else if (d.hensachi.source_url) d.data_status = 'verified';
  }
  return diffs;
}

function writeReport(research, diffs, uniName) {
  fs.mkdirSync(VERIF_DIR, { recursive: true });
  // 機械可読ログ（追記）
  const logPath = path.join(VERIF_DIR, 'diff_log.jsonl');
  const lines = diffs.map(x => JSON.stringify({ ...x, uni_id: research.uni_id, date: research.researched_date }));
  fs.appendFileSync(logPath, lines.join('\n') + (lines.length ? '\n' : ''), 'utf8');
  // 人間可読レポート（上書き）
  const md = [
    `# ${uniName}（${research.uni_id}）照合レポート`,
    ``,
    `調査日: ${research.researched_date} / 変更点: ${diffs.length}件`,
    ``,
    `## 出典`,
    ...(research.sources || []).map(s => `- ${s}`),
    ``,
    `## 変更差分`,
    diffs.length ? '| 学部 | 項目 | 旧 | 新 |' : '（差分なし＝現行値が正確だった）',
    ...(diffs.length ? ['|---|---|---|---|'] : []),
    ...diffs.map(x => `| ${x.dept_id} | ${x.field} | ${JSON.stringify(x.old)} | ${JSON.stringify(x.new)} |`),
    ``,
    ...(research.notes ? [`## 特記事項`, research.notes] : []),
  ].join('\n');
  fs.writeFileSync(path.join(VERIF_DIR, `${research.uni_id}.md`), md, 'utf8');
}

function main() {
  const files = process.argv.slice(2);
  if (!files.length) { console.error('使い方: node merge_research.js research/<uni_id>.json ...'); process.exit(1); }
  const dataPath = path.join(WORK, 'data_v2.json');
  const data = JSON.parse(fs.readFileSync(dataPath, 'utf8'));
  for (const f of files) {
    const research = JSON.parse(fs.readFileSync(path.join(WORK, f), 'utf8'));
    const uni = data.universities.find(u => u.id === research.uni_id);
    if (!uni) throw new Error(`不明な uni_id: ${research.uni_id}`);
    const diffs = mergeUni(data, research);
    writeReport(research, diffs, uni.name);
    console.log(`${research.uni_id}: ${diffs.length}件の差分を反映`);
  }
  fs.writeFileSync(dataPath, JSON.stringify(data, null, 1), 'utf8');
  console.log('data_v2.json 更新完了');
}

module.exports = { mergeUni };
if (require.main === module) main();
```

- [ ] **Step 4: テストが通ることを確認**

```bash
node test_merge_research.js
```

Expected: `ALL OK`

- [ ] **Step 5: コミット**

```bash
git add work/merge_research.js work/test_merge_research.js
git commit -m "feat: 調査結果マージ＋差分レポート生成"
```

---

### Task 5: 調査プロンプトテンプレートと運用README

**Files:**
- Create: `docs/data-verification/PROMPT_TEMPLATE.md`
- Create: `docs/data-verification/README.md`
- Create: `work/research/`（空ディレクトリ、.gitkeep）

- [ ] **Step 1: README.md を作成**

```markdown
# データ検証台帳（v2）

v2データ（work/data_v2.json）の実データ検証の記録。**2027年3月の年次更新時はこのフォルダを作業台帳として使う。**

## 構成
- `PROMPT_TEMPLATE.md` — 調査エージェントに渡すプロンプトの雛形
- `<uni_id>.md` — 大学ごとの照合レポート（調査日・出典・変更差分）
- `diff_log.jsonl` — 全変更の機械可読ログ（追記式）

## 運用フロー（1バッチ = 20〜30校）
1. `work/dept_manifest.json` から対象校の学部一覧を切り出す
2. PROMPT_TEMPLATE.md に大学名・学部一覧を埋めて調査エージェントを並列起動（1エージェント3〜5校）
3. エージェント出力を `work/research/<uni_id>.json` に保存
4. `node merge_research.js research/<uni_id>.json ...` でマージ
5. `node check_v2.js` で検収 → コミット

## 情報源の優先順（spec準拠）
1. 河合塾Kei-Net（偏差値の正）
2. 大学公式サイト（入試方式・倍率・ゼミ・資格）
3. パスナビ等の進学情報サイト（補完）

## 原則
- 取れなかった値は無理に埋めない（unconfirmed運用）
- 全値に source_url を付ける。付けられない値は書かない
- ゼミは学部あたり最大3件・看板を先頭に。深掘り調査はしない
```

- [ ] **Step 2: PROMPT_TEMPLATE.md を作成**

````markdown
# 調査エージェント用プロンプト雛形

以下を1エージェントあたり3〜5校分埋めて使う。

---

あなたは日本の大学入試データの調査員です。以下の大学について、指定スキーマのJSONを作成してください。

## 対象大学と学部一覧（dept_idはこれを厳守。改変・追加・省略しない）

{{dept_manifest.json から対象校分を貼り付け}}

## 調査項目（学部ごと）
1. **偏差値**: 河合塾Kei-Net（https://www.keinet.ne.jp/）の最新値を最優先。学部内に複数学科があれば min/max で幅を表現。2.5刻み
2. **一般入試倍率**: 大学公式サイトの入試結果ページから直近2年分。学部単位（学科は代表値または平均）
3. **入試方式**: 総合型/公募推薦/一般/共通テスト利用の有無（学部単位で判明する範囲）
4. **看板ゼミ・研究室**: 学部公式ページで紹介されている代表的なゼミ・研究室を1件（容易に見つかれば最大3件、看板を先頭に）。名称＋テーマ1行＋掲載ページURL
5. **主な就職系統**: 学部の進路実績ページから業界タグを2〜4個（許可ID: koumuin, kyoiku, kinyu, maker, it, ryutsu, iryo_fukushi, kensetsu, media, service, shokuhin, sports_kenko）

## 厳守事項
- **すべての値に source_url を付ける。出典が確認できない値は出力しない**（フィールドごと省略する）
- 推測で埋めない。見つからなければ省略、学部ごと信頼できる情報が皆無なら "unconfirmed": true
- ゼミ調査に1学部あたり数分以上かけない（見つからなければ即スキップ）
- 出力は下記スキーマのJSONのみ（1大学=1オブジェクト、複数校は配列で）

## 出力スキーマ

```json
{
  "uni_id": "対象のuni_id",
  "researched_date": "YYYY-MM-DD（今日）",
  "sources": ["主要な出典URL一覧"],
  "departments": [
    {
      "dept_id": "manifest記載のID",
      "hensachi": { "min": 50.0, "max": 55.0, "source_url": "..." },
      "bairitsu": { "ippan": { "2025": 3.4, "2026": 3.1 }, "source_url": "..." },
      "entry_methods": [ { "type": "kyotsu", "available": true, "note": "..." } ],
      "employment_fields": ["kinyu"],
      "zemi": [ { "name": "...", "theme": "...", "source_url": "..." } ],
      "unconfirmed": false
    }
  ],
  "notes": "特記事項（学部改組の予定、データの注意点など）"
}
```
````

- [ ] **Step 3: research ディレクトリ作成とコミット**

```bash
mkdir -p work/research && touch work/research/.gitkeep
git add docs/data-verification/ work/research/.gitkeep
git commit -m "docs: 調査プロンプト雛形と検証台帳README"
```

---

### Task 6〜10: Web調査バッチ 1〜5（各20〜30校）

**Files:**
- Create: `work/research/<uni_id>.json`（バッチ対象校分）
- Modify: `work/data_v2.json`（マージにより更新）
- Create: `docs/data-verification/<uni_id>.md`（自動生成）

**バッチ分割**（dept_manifest.json の大学出現順で機械的に分割）:
- バッチ1（Task 6）: 1〜25校目
- バッチ2（Task 7）: 26〜50校目
- バッチ3（Task 8）: 51〜75校目
- バッチ4（Task 9）: 76〜100校目
- バッチ5（Task 10）: 101〜123校目

**各バッチの手順（Task 6〜10 共通。以下の全ステップを毎バッチ実施する）:**

- [ ] **Step 1: バッチ対象校リストを生成**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版/work"
node -e "
const m = require('./dept_manifest.json');
const unis = [...new Set(m.map(x => x.uni_id))];
const BATCH = 1;               // ★バッチ番号を書き換える（1〜5）
const SIZE = 25;
const target = unis.slice((BATCH-1)*SIZE, BATCH*SIZE);
console.log(JSON.stringify(target, null, 1));
console.log('校数:', target.length);
const fs = require('fs');
fs.writeFileSync('research/_batch' + BATCH + '.json', JSON.stringify(
  target.map(id => ({ uni_id: id, depts: m.filter(x => x.uni_id === id) })), null, 1));
"
```

Expected: 対象校リストが表示され `research/_batchN.json` が生成される。

- [ ] **Step 2: 調査エージェントを並列起動**

実行セッションで Agent tool（general-purpose、WebSearch/WebFetch可）を並列起動する。1エージェント=3〜5校、同時5〜8体。プロンプトは `docs/data-verification/PROMPT_TEMPLATE.md` に `research/_batchN.json` の該当校データを埋めたもの。エージェントの返答JSONを `work/research/<uni_id>.json` に保存する。

- [ ] **Step 3: 保存ファイルの実在と形式を確認**

```bash
node -e "
const fs = require('fs');
const batch = require('./research/_batch1.json');  // ★バッチ番号
for (const b of batch) {
  const p = 'research/' + b.uni_id + '.json';
  if (!fs.existsSync(p)) { console.log('MISSING:', p); continue; }
  const r = JSON.parse(fs.readFileSync(p, 'utf8'));
  console.log(b.uni_id, ': depts=', (r.departments||[]).length, '/', b.depts.length);
}
"
```

Expected: 全校のファイルが存在し、学部数がマニフェストと近い（欠けはエージェント再実行）。

- [ ] **Step 4: マージ実行**

```bash
node merge_research.js $(node -e "
const b = require('./research/_batch1.json');      // ★バッチ番号
console.log(b.map(x => 'research/' + x.uni_id + '.json').join(' '));
")
```

Expected: 各校の差分件数が表示され `data_v2.json 更新完了`。dept_id不一致エラーが出たら該当調査JSONを修正して再実行。

- [ ] **Step 5: 検収チェック**

```bash
node check_v2.js
```

Expected: `CHECK PASS`。verified数がバッチ分増えている。

- [ ] **Step 6: 差分レポートを目視確認**

`docs/data-verification/` の当該バッチ校の .md を開き、明らかな異常（偏差値±15以上の乖離、倍率100倍等）がないか確認。異常があれば出典を確認して調査JSONを修正→再マージ。

- [ ] **Step 7: コミット**

```bash
git add work/research/ work/data_v2.json docs/data-verification/
git commit -m "data: 調査バッチN反映（○○校、verified△△学部）"
```

**注意**: 1バッチ=1セッションを想定。セッションをまたぐ場合は次セッション冒頭で `git log --oneline -3` と `node check_v2.js` で現在地を確認してから再開する。

---

### Task 11: 最終検収とフェーズ1完了報告

**Files:**
- Create: `docs/data-verification/_summary.md`

- [ ] **Step 1: 全体統計を集計**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版/work"
node check_v2.js
node -e "
const d = require('./data_v2.json');
const fs = require('fs');
const total = d.departments.length;
const v = d.departments.filter(x => x.data_status === 'verified').length;
const u = d.departments.filter(x => x.data_status === 'unconfirmed').length;
const z = d.departments.filter(x => x.zemi.length > 0).length;
const zTotal = d.departments.reduce((s, x) => s + x.zemi.length, 0);
const ef = d.departments.filter(x => x.employment_fields.length > 0).length;
const b = d.departments.filter(x => Object.keys(x.bairitsu.ippan).length > 0).length;
const lines = [
  '# フェーズ1 最終検収サマリー',
  '',
  '集計日: ' + new Date().toISOString().slice(0,10),
  '',
  '| 指標 | 値 |', '|---|---|',
  '| 大学数 | ' + d.universities.length + ' |',
  '| 学部数 | ' + total + ' |',
  '| verified | ' + v + ' (' + (v/total*100).toFixed(1) + '%) |',
  '| unconfirmed | ' + u + ' |',
  '| ゼミあり学部 | ' + z + ' (' + (z/total*100).toFixed(1) + '%) / 延べ' + zTotal + '件 |',
  '| 倍率実データあり | ' + b + ' |',
  '| 就職系統タグあり | ' + ef + ' |',
];
fs.writeFileSync('../docs/data-verification/_summary.md', lines.join('\n'), 'utf8');
console.log(lines.join('\n'));
"
```

Expected: サマリー表示＋`_summary.md` 生成。目標: verified 8割以上、ゼミカバー率6〜8割。

- [ ] **Step 2: v1が無変更であることを確認**

```bash
cd "/c/Users/togan1080/OneDrive/Claude/進路診断_一般版"
git log --oneline --all -- 進路診断_一般版.html work/app.html work/build.js | head -3
ls -la 進路診断_一般版.html
```

Expected: v1関連ファイルへの変更コミットがない（Task 0の初期スナップショットのみ）。

- [ ] **Step 3: 全テスト再実行**

```bash
cd work && node test_to_v2.js && node test_check_v2.js && node test_merge_research.js && node check_v2.js
```

Expected: `ALL OK` ×3 ＋ `CHECK PASS`

- [ ] **Step 4: 最終コミットと完了報告**

```bash
git add -A && git commit -m "docs: フェーズ1最終検収サマリー"
```

ユーザーに報告: 検収サマリー（verified率・ゼミカバー率・unconfirmed一覧の傾向）と、フェーズ2（診断エンジンv2）への引き継ぎ事項（interest_tagsタクソノミー確定→リタグ、unconfirmed学部の扱い）。

---

## セルフレビュー結果

- **Spec coverage**: 2層スキーマ=Task 2 / 実データ検証（出典・検証日）=Task 4,6-10 / 看板ゼミ全学部×1件（最大3件）=Task 4のマージ規則＋PROMPT_TEMPLATE / タグ定義表=Task 1 / 検収基準の機械チェック=Task 3 / 照合レポート・作業台帳=Task 4,5 / v1無変更=Task 11 Step 2 — 全要件にタスクあり
- **暫定タクソノミーのリタグ容易性**: tags_def.jsonのIDを不変とし、フェーズ2でmapping tableによる一括リタグ（specどおり）
- **型整合**: dept レコードの形は test_to_v2.js / test_check_v2.js / test_merge_research.js の makeDept で同一構造を使用。entry_methods の type 名（sogo/koubo/ippan/kyotsu）は全タスクで統一
